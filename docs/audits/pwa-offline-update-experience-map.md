# 1. Executive summary

- **Current PWA architecture**: Uses a basic, manually-written service worker (`public/sw.js`) and a hardcoded manifest (`public/manifest.json`). The service worker is registered on window load in `src/routes/__root.tsx`.
- **Current installability**: The application has a manifest with `display: standalone` and icons, and is installable as a PWA, but there are no custom in-app install prompts.
- **Current service-worker architecture**: The service worker intercepts GET requests. It uses a cache-first strategy for static assets and a network-first strategy for navigation requests. Server and AI functions are excluded from caching.
- **Current cache strategy**: Hybrid. Navigation is network-first with a fallback to the cached root `/`. Static assets (images, CSS, JS) are cache-first. API calls (like `/_server` and `ai.functions`) are network-only.
- **Current offline capabilities**: The app shell can load offline via the network-first fallback to `/`. However, because API calls are network-only and not queued, dynamic data fetching and mutation fail offline.
- **Current update experience**: The service worker calls `skipWaiting()` immediately on install. However, the application code does not listen for `updatefound` or controller changes. There is no user prompt to reload when an update is available.
- **Strongest implemented areas**: Basic installability and offline app shell loading.
- **Most serious stale-cache risks**: The cache name `fitcore-v1` is hardcoded. New deployments do not invalidate the old cache unless the name is manually bumped in `public/sw.js`.
- **Most serious offline data risks**: Mutations and dynamic data requests are network-only without offline queueing or explicit error surfaces, leading to silent failures or complete unavailability of features when offline.
- **Most serious update risks**: Users may remain stuck on a stale cached version of the app because there is no update prompt or automatic reload mechanism integrated into the client app.
- **Most serious platform-specific gaps**: No custom handling for iOS safe areas or Android maskable icon paddings beyond standard manifest fields.
- **Major future Data Safety dependencies**: Future offline mutation queueing, atomic local saves, and sync mechanisms.
- **Most important requirements for future implementation approval**: Implementing a robust, automated asset caching and versioning strategy (e.g., via Workbox) and adding a user-facing update prompt.

# 2. Method and evidence boundaries

- **Required base SHA**: `3e4326782d761313c4f2644ecfe55503770b360a`
- **Static code-and-test inspection methodology**: The audit relies exclusively on static analysis of the source code, manifests, and existing markdown documentation.
- **Files inspected**: `public/manifest.json`, `public/sw.js`, `src/routes/__root.tsx`, `vite.config.ts`, `docs/audits/service-worker-cache-audit.md`.
- **Tests inspected**: E2E smoke tests in `tests/e2e/smoke.spec.ts`.
- **Why installability is not being claimed as runtime verified**: No browser automation or Lighthouse audits were run.
- **Why offline behavior is not being claimed as runtime verified**: No network simulation was run.
- **Why update behavior is not being claimed as runtime verified**: No service-worker lifecycle simulation was performed.
- **How build-time configuration is distinguished from runtime behavior**: Analyzed Vite configuration and how `public/sw.js` is served statically rather than generated by a plugin.
- **How current main is distinguished from unmerged Data Safety work**: Only the checked-out branch at the specified SHA was inspected. Parked PRs were explicitly ignored.

- **Confirmed implemented**: Visible in current main source code.
- **Confirmed missing**: Confirmed to not exist in current main source code.
- **Confirmed partial**: Exists but lacks full capability.
- **Configuration present**: Declared in configuration files (e.g., manifest, HTML meta).
- **Probable risk**: Logical conclusion based on static analysis.
- **Platform dependent**: Requires specific OS/browser to verify.
- **Requires browser verification**: Needs runtime testing.
- **Future dependency**: Depends on unmerged or planned architectural changes.
- **Not applicable**: Not relevant to this codebase.
- **Unclear**: Cannot be determined statically.

# 3. PWA architecture map

- **HTML entry point**: Handled by TanStack Start / Vite SSR. The root HTML shell is defined in `src/routes/__root.tsx`.
- **Manifest**: `public/manifest.json` (static file).
- **Service worker**: `public/sw.js` (handwritten static file).
- **Service-worker registration**: `src/routes/__root.tsx` (via `ServiceWorkerRegister` component).
- **Application bootstrap**: TanStack Router initializes in `src/routes/__root.tsx`.
- **Asset pipeline**: Managed by Vite/Nitro as configured in `vite.config.ts`.
- **Public assets**: Stored in `public/` and served at the root path.
- **Routing**: Managed by `@tanstack/react-router`.
- **Cache storage**: Managed manually within `public/sw.js`.
- **Local persistence**: Direct `localStorage` usage (e.g., in `src/lib/persist.ts`, `src/lib/fitcore-data.ts`).
- **Install prompt handling**: Confirmed missing.
- **Update handling**: Confirmed missing.
- **Offline indicators**: Confirmed missing.
- **Build-time plugins or generators**: Vite config does not currently use a PWA plugin.

| File                    | Purpose                   | Runtime Role   | Status      | Consumers | Lifecycle    | Test Coverage | Preservation Requirement |
| :---------------------- | :------------------------ | :------------- | :---------- | :-------- | :----------- | :------------ | :----------------------- |
| `src/routes/__root.tsx` | Entry and SW registration | Bootstrap      | Handwritten | Browser   | Load         | E2E Smoke     | High                     |
| `public/sw.js`          | Cache & offline routing   | Service Worker | Handwritten | Browser   | SW Lifecycle | Absent        | High                     |
| `public/manifest.json`  | PWA Metadata              | Installability | Handwritten | Browser   | Install      | Absent        | High                     |

# 4. Manifest audit

- **`name`**: `FitCore` (Source: `public/manifest.json`, Purpose: App install name, Platform relevance: All, Completeness: Yes, Risk: Low, Verification: None)
- **`short_name`**: `FitCore` (Source: `public/manifest.json`, Purpose: Home screen name, Platform relevance: All, Completeness: Yes, Risk: Low, Verification: None)
- **`description`**: `Personal fitness command center with AI coaching.` (Source: `public/manifest.json`, Purpose: App description, Platform relevance: All, Completeness: Yes, Risk: Low, Verification: None)
- **`start_url`**: `/` (Source: `public/manifest.json`, Purpose: Entry point on launch, Platform relevance: All, Completeness: Yes, Risk: Low, Verification: None)
- **`display`**: `standalone` (Source: `public/manifest.json`, Purpose: UI mode, Platform relevance: All, Completeness: Yes, Risk: Low, Verification: None)
- **`background_color`**: `#0b0a0f` (Source: `public/manifest.json`, Purpose: Splash screen background, Platform relevance: All, Completeness: Yes, Risk: Low, Verification: None)
- **`theme_color`**: `#1a1626` (Source: `public/manifest.json`, Purpose: Status bar/browser UI color, Platform relevance: All, Completeness: Yes, Risk: Low, Verification: None)
- **`orientation`**: `portrait-primary` (Source: `public/manifest.json`, Purpose: Lock orientation, Platform relevance: Mobile, Completeness: Yes, Risk: Low, Verification: Requires browser verification)
- **`icons`**: Array of 4 icons (192, 512, maskable variants). (Source: `public/manifest.json`, Purpose: App icons, Platform relevance: All, Completeness: Yes, Risk: Low, Verification: None)

Missing fields: `scope`, `display_override`, `lang`, `dir`, `id`, `categories`, `shortcuts`, `screenshots`, `share_target`, `protocol_handlers`, `file_handlers`, `launch_handler`, related applications.

# 5. Icon and branding asset audit

- **`public/icon-192.png`**: 192x192, PNG, Manifest reference, Standard manifest icon, All platforms. No transparent-padding risk observed statically. Test coverage absent.
- **`public/icon-512.png`**: 512x512, PNG, Manifest reference, Standard manifest icon, All platforms. Test coverage absent.
- **`public/icon-maskable-192.png`**: 192x192, PNG, Manifest reference (`purpose: maskable`), Android maskable icon. Maskable-area risk unclear without browser verification. Test coverage absent.
- **`public/icon-maskable-512.png`**: 512x512, PNG, Manifest reference (`purpose: maskable`), Android maskable icon. Maskable-area risk unclear without browser verification. Test coverage absent.
- **`public/apple-touch-icon.png`**: PNG, HTML reference, iOS home screen icon. Test coverage absent.

Duplicate or missing variants: No splash screen images are explicitly provided for iOS. Notification icons are missing.

# 6. HTML metadata audit

Inspected `src/routes/__root.tsx`:

- **viewport**: `width=device-width, initial-scale=1, viewport-fit=cover, maximum-scale=1` (Source: `src/routes/__root.tsx`, Platform: All, Consistency: N/A, Risk: Maximum-scale restricts zoom, Verification: Requires browser verification)
- **theme color**: `#1a1626` (Source: `src/routes/__root.tsx`, Platform: All, Consistency: Matches manifest, Risk: Low)
- **Apple mobile web app capability**: `yes` (Source: `src/routes/__root.tsx`, Platform: iOS, Consistency: N/A, Risk: Low)
- **Apple status-bar style**: `black-translucent` (Source: `src/routes/__root.tsx`, Platform: iOS, Consistency: N/A, Risk: Low)
- **Apple touch icon**: `/apple-touch-icon.png` (Source: `src/routes/__root.tsx`, Platform: iOS, Consistency: N/A, Risk: Low)
- **manifest link**: `/manifest.json` (Source: `src/routes/__root.tsx`, Platform: All, Consistency: N/A, Risk: Low)
- **description**: Missing in `<meta>` (JSON-LD provides description instead).
- **title**: `FitCore` (via `application-name` and `apple-mobile-web-app-title`).
- **color scheme**: Not explicitly defined in `<meta>`. HTML tag uses `className="dark"`.

# 7. Service-worker registration audit

- **registration trigger**: `window.addEventListener("load")` if `"serviceWorker" in navigator`.
- **environment conditions**: Registers unconditionally in all environments.
- **production-only behavior**: None.
- **registration scope**: Default (based on script location `/sw.js`).
- **registration success**: Logs `"SW registered: "` to console.
- **registration failure**: Logs `"SW registration failed: "` to console.
- **update checks**: Confirmed missing.
- **`updatefound`**: Confirmed missing.
- **installing worker**: Handled natively, no app-level tracking.
- **waiting worker**: Handled natively, no app-level tracking.
- **active worker**: Handled natively, no app-level tracking.
- **controller changes**: Confirmed missing.
- **unregister behavior**: Confirmed missing.
- **logging**: Basic `console.log` on success/error.
- **user-facing feedback**: Confirmed missing.

| Stage    | Source File             | Handler                            | Visible Behavior | Failure Behavior | Test Coverage | Risk |
| :------- | :---------------------- | :--------------------------------- | :--------------- | :--------------- | :------------ | :--- |
| Load     | `src/routes/__root.tsx` | `window.addEventListener("load")`  | None             | Console log      | Absent        | Low  |
| Register | `src/routes/__root.tsx` | `navigator.serviceWorker.register` | None             | Console log      | Absent        | High |

# 8. Service-worker lifecycle audit

- **install**: Caches hardcoded `STATIC_ASSETS`, then calls `self.skipWaiting()`.
- **precache**: Hardcoded array `STATIC_ASSETS` in `public/sw.js`.
- **activation**: Deletes caches whose name does not match `CACHE_NAME` (`fitcore-v1`), then calls `self.clients.claim()`.
- **old cache cleanup**: Performed during activation.
- **waiting**: Skipped due to `self.skipWaiting()`.
- **`skipWaiting`**: Called immediately on install.
- **client claiming**: Called immediately on activate.
- **controller change**: Handled natively, but app does not listen to `controllerchange` to reload.
- **update activation**: Activates immediately.
- **service-worker failure**: No explicit fallback.
- **worker replacement**: Automatic due to `skipWaiting()`.

| Lifecycle Stage | Source         | Event Handler | Cache Effect           | Client Effect  | User-Facing Effect | Test Coverage | Risk |
| :-------------- | :------------- | :------------ | :--------------------- | :------------- | :----------------- | :------------ | :--- |
| Install         | `public/sw.js` | `install`     | Populates `fitcore-v1` | None           | None               | Absent        | Low  |
| Activate        | `public/sw.js` | `activate`    | Cleans old caches      | Claims clients | None               | Absent        | High |

New versions activate immediately without explicit user action.

# 9. Cache inventory

- **name**: `fitcore-v1`
- **versioning strategy**: Hardcoded string in `public/sw.js`.
- **source file**: `public/sw.js`
- **contents**: Root `/`, manifest, icons, and dynamic matches for assets (`.js`, `.css`, `.png`, etc.).
- **population trigger**: On `install` for static assets; on `fetch` for runtime assets.
- **cleanup trigger**: On `activate` (deletes non-matching cache names).
- **update strategy**: Cache first for static, Network first for navigation.
- **expiration behavior**: None implemented.
- **storage-growth risk**: High. Runtime cache accumulates indefinitely since there is no expiration logic.
- **test coverage**: Absent.

Distinction:

- precache: `STATIC_ASSETS` list.
- static runtime cache: Accumulated dynamically on `fetch`.
- navigation cache: Not cached dynamically, relies on the pre-cached `/`.
- API cache: Explicitly excluded.

# 10. Cache-strategy audit

- **Navigation documents (`mode === 'navigate'`)**: Network first, fallback to cached `/`. Source: `public/sw.js`. Stale-content risk: High (if fallback occurs). Offline behavior: Returns cached shell. Test coverage: Absent.
- **Static assets (`.js`, `.css`, `.png`, etc.)**: Cache first, fallback to network. Source: `public/sw.js`. Updates dynamically into `fitcore-v1`. Stale-content risk: Medium (Vite hashes filenames, but cached HTML may point to dead hashed names). Test coverage: Absent.
- **Server functions (`/_server`)**: Network only. Source: `public/sw.js`. Offline behavior: Fails. Test coverage: Absent.
- **AI functions (`ai.functions`)**: Network only. Source: `public/sw.js`. Offline behavior: Fails. Test coverage: Absent.
- **External origins**: Network only. Source: `public/sw.js`. Offline behavior: Fails. Test coverage: Absent.

# 11. Navigation and SPA fallback audit

- **direct route entry**: Network first, falls back to `/`.
- **browser reload on nested routes**: Network first, falls back to `/`.
- **unknown routes**: TanStack Router handles `404` client-side via `NotFoundComponent` in `src/routes/__root.tsx`.
- **offline navigation**: Client-side routing succeeds if JS bundles are cached. Hard reload falls back to `/`.
- **navigation preload**: Confirmed missing.
- **cached application shell**: `/` is cached on install.
- **index fallback**: Used on network failure for `navigate` requests.
- **error fallback**: No specific SW error page; returns cached `/`.

Deep links survive offline reload if the JS bundle for that route is already in the runtime cache.

# 12. Offline startup audit

- **first visit is offline**: Fails (no SW installed).
- **returning user is offline**: Loads cached `/` shell.
- **cached shell exists**: Loads successfully.
- **cached shell does not exist**: White screen or browser offline page.
- **service worker is not yet installed**: Fails.
- **manifest is cached**: Yes (in `STATIC_ASSETS`).
- **scripts are cached**: Yes (via Cache First).
- **storage data exists**: Extracted from `localStorage` client-side.
- **storage data is unavailable**: Initial states apply.
- **a new version is waiting**: Not applicable (`skipWaiting` prevents waiting state).

First-visit offline support is confirmed missing.

# 13. Offline navigation audit

Offline navigation across surfaces (Home, Training, Fuel/Nutrition, Recovery, Stats/Progress, Jarvis, Settings):

- **static asset dependency**: High. Requires JS chunks for each route to be cached.
- **data dependency**: High for initial load. Uses TanStack Query, which may cache data in memory, but server functions are network-only.
- **current offline availability**: App shell and previously visited routes remain navigable. Unvisited routes will fail if their JS chunks aren't cached.
- **likely missing asset risk**: High for deep routes on a fresh install before they are visited.
- **likely stale-data risk**: Low for UI, but data won't fetch.
- **user-facing indicator**: Confirmed missing.
- **tests**: Absent.

# 14. Offline data-entry audit

- **network dependency**: Most mutations appear to persist to `localStorage` (via `src/lib/fitcore-data.ts`), making them locally available.
- **local persistence dependency**: High (synchronous `localStorage` writes).
- **user-facing behavior**: Mutations save to `localStorage`.
- **success feedback**: Local state updates immediately.
- **failure feedback**: None specific to network; quota failures are silently caught in `src/lib/persist.ts`.
- **reconnect behavior**: Confirmed missing.
- **duplicate-risk behavior**: Unclear without Data Safety sync implementation.
- **test coverage**: Absent for offline specifically.
- **future Data Safety dependency**: High. Offline queuing and sync to a cloud backend are future requirements.

# 15. Reconnect and online-state audit

- **online → offline**: No visible feedback.
- **offline → online**: No visible feedback.
- **intermittent connection**: Handled gracefully by TanStack Query retries, but no app-wide UI.
- **service-worker-controlled → uncontrolled**: Unlikely.
- **stale page → new deployment available**: Remains stale until hard refresh.

Inspected events: `navigator.onLine`, `online`, `offline`.
Confirmed missing: Event listeners for network state transitions, automatic resynchronization, user messaging.

# 16. Update-detection audit

- **periodic registration update**: Confirmed missing.
- **page-load update check**: Handled natively by the browser on navigation.
- **`updatefound`**: Confirmed missing.
- **waiting worker detection**: Confirmed missing.
- **build version comparison**: Confirmed missing.
- **cache-version comparison**: Confirmed missing.
- **controller change**: Confirmed missing.
- **manual refresh**: Only way to update the app.
- **app version display**: Confirmed missing.

Users may remain indefinitely on stale code.

# 17. Waiting-worker and update-prompt audit

- **prompt visibility**: Confirmed missing.
- **message**: Confirmed missing.
- **Refresh action**: Confirmed missing.
- **Later action**: Confirmed missing.
- **automatic activation**: Yes (`skipWaiting`).
- **state retention**: Not applicable.
- **unsaved-form protection**: None.
- **active-workout protection**: None.

Because `skipWaiting` is used and there is no prompt, if a user reloads, the new SW takes over. If the browser forcefully reloads the page upon activation, active workouts and unsaved forms are lost.

# 18. Activation and reload audit

- **`skipWaiting`**: Called immediately on install in `public/sw.js`.
- **controller change**: App does not listen for it.
- **automatic reload**: The app does not trigger `window.location.reload()`.
- **manual reload**: Requires user action.
- **multiple-client behavior**: Unpredictable if clients are on different versions.
- **active-workout behavior**: Unprotected.
- **unsaved-form behavior**: Unprotected.

Activation is automatic (`skipWaiting`), but reload is mixed/unclear (relies on browser behavior or manual user refresh).

# 19. Cache invalidation audit

- **cache-name versioning**: Hardcoded as `fitcore-v1`.
- **old-cache deletion**: Handled in `activate`, but since the name doesn't change automatically, it never triggers for new deployments unless manually bumped.
- **hashed asset URLs**: Used by Vite.
- **stale-asset risk**: High. If the HTML shell falls back to the cache, it may request old hashed JS files.
- **storage-growth risk**: High. Old hashed files are never purged from `fitcore-v1` because the runtime cache grows indefinitely.

# 20. Deployment and version-consistency audit

- **HTML from one release**: Possible if offline fallback is used.
- **JavaScript from another release**: Possible if a new deployment occurs while a user has the app open.
- **stale service worker**: Common, due to lack of update flow.
- **stale manifest**: Possible.
- **current prevention**: None.
- **current detection**: None.
- **fallback**: None.
- **future Data Safety dependency**: High.

# 21. Storage and cache quota audit

- **Cache Storage**: Grows indefinitely with runtime assets.
- **localStorage**: Used extensively for state and data (`src/lib/persist.ts`, `src/lib/fitcore-data.ts`).
- **quota handling**: `try/catch` in `src/lib/persist.ts` catches quota errors silently.
- **visible error**: Confirmed missing.
- **test coverage**: Absent.
- **risk**: High. Silent failures on quota exceeded.

# 22. Service-worker error audit

- **registration failure**: Logged to console (`src/routes/__root.tsx`).
- **install failure**: Unhandled.
- **precache failure**: Unhandled.
- **activate failure**: Unhandled.
- **fetch-handler failure**: Network-first navigation falls back to `/`. Cache-first assets fall back to network.
- **user-facing feedback**: Confirmed missing.
- **fallback**: Network-first navigation uses `caches.match('/')`.

# 23. Offline fallback and error-surface audit

- **offline banner**: Confirmed missing.
- **offline page**: Confirmed missing.
- **cached-data notice**: Confirmed missing.
- **failed-network message**: `ErrorComponent` in `src/routes/__root.tsx` provides a generic "This page didn't load" message with a "Try again" button.
- **retry button**: Present in `ErrorComponent`.
- **tests**: Absent.

# 24. Install-prompt audit

- **`beforeinstallprompt`**: Confirmed missing.
- **deferred prompt storage**: Confirmed missing.
- **install button**: Confirmed missing.
- **app-installed event**: Confirmed missing.
- **browser-specific behavior**: Relies entirely on native browser prompts.

# 25. Standalone-mode audit

- **display-mode detection**: Confirmed missing in code (no `window.matchMedia('(display-mode: standalone)')` found).
- **top safe area**: Configured via `viewport-fit=cover` in HTML meta.
- **bottom safe area**: Relies on general CSS.
- **back behavior**: Handled natively by OS.
- **refresh behavior**: No pull-to-refresh implementation visible.
- **test coverage**: Absent.

# 26. iOS-specific PWA audit

- **Apple touch icons**: `/apple-touch-icon.png` is referenced in HTML.
- **`apple-mobile-web-app-capable`**: `yes` is set in HTML meta.
- **status-bar style**: `black-translucent` is set in HTML meta.
- **safe-area usage**: `viewport-fit=cover` is set.
- **splash behavior**: No `apple-touch-startup-image` metadata found.
- **test coverage**: Absent.
- **risk**: Medium. iOS aggressively evicts `localStorage` for PWAs not used frequently.

# 27. Android-specific PWA audit

- **maskable icons**: Present in `public/manifest.json`.
- **theme color**: `#1a1626` matches HTML.
- **install prompt**: Relies on native prompt.
- **test coverage**: Absent.
- **risk**: Low, standard manifest support is sufficient.

# 28. Desktop-installation audit

- **desktop manifest support**: Standard `standalone` applies.
- **minimum window behavior**: Confirmed missing.
- **standalone navigation**: Standard OS window chrome.
- **test coverage**: Absent.
- **responsive risk**: Low, app uses Tailwind responsive classes.

# 29. Manifest shortcut and advanced capability audit

- **shortcuts**: Confirmed missing.
- **share target**: Confirmed missing.
- **file handlers**: Confirmed missing.
- **protocol handlers**: Confirmed missing.
- **launch handler**: Confirmed missing.
- **screenshots**: Confirmed missing.
- **push notifications**: Confirmed missing.
- **background sync**: Confirmed missing.

# 30. External-resource dependency audit

- **fonts**: `fonts.googleapis.com` and `fonts.gstatic.com` (Preconnected in HTML, fetched Network-first via SW).
- **CDN assets**: None explicitly listed outside fonts.
- **analytics services**: None visible in core bootstrap.
- **failure behavior**: Fonts fallback to system defaults.

# 31. Progress-photo and image offline audit

- **storage mechanism**: Unclear statically, likely `localStorage` base64 or IndexedDB.
- **offline support**: Fails if relying on network endpoints; succeeds if local `localStorage` is used.
- **quota risk**: High if base64 images are stored in `localStorage`.
- **future Data Safety dependency**: High.

# 32. Jarvis offline audit

- **network dependency**: High. AI calls (`ai.functions`) are Network Only in `public/sw.js`.
- **local dependency**: High. Reads `AI_DIAGNOSTICS_STORAGE` from `localStorage` in `src/components/app/jarvis/jarvis-panel.tsx`.
- **current fallback**: Fails offline (Network Only in SW). No offline queueing.
- **tests**: Absent for offline behavior.

# 33. Active-workout update-safety audit

- **current update behavior**: `skipWaiting` activates SW immediately.
- **current reload behavior**: Manual only.
- **possible data-loss risk**: High. If the browser forcefully reloads the tab due to memory pressure or a forced update, in-memory active workout state is lost.
- **user warning**: Confirmed missing.
- **future Data Safety dependency**: High.

# 34. Unsaved-form update-safety audit

- **current draft retention**: Relies on `localStorage` synced state.
- **update prompt behavior**: Confirmed missing.
- **reload consequence**: Depends on sync frequency.
- **warning**: Confirmed missing.
- **risk**: High.

# 35. Privacy and security-boundary audit

- **sensitive data stored locally**: Yes, in `localStorage`.
- **cache persistence after logout or reset**: Cached SW assets persist.
- **offline access to sensitive data**: Possible if device is compromised.
- **test coverage**: Absent.

# 36. Accessibility audit for install, offline, and update surfaces

- **install controls**: Absent.
- **offline banners**: Absent.
- **update prompts**: Absent.
- **retry buttons**: Present in `ErrorComponent`. Semantically a `<button>`.
- **classification**: Absent for custom controls.

# 37. Responsive audit for install, offline, and update surfaces

- **source**: `ErrorComponent` uses standard Tailwind classes (`max-w-md`, `px-4`).
- **viewport concern**: Low.
- **test coverage**: Absent.
- **verification priority**: Low.

# 38. Service-worker and PWA test-coverage map

- **tests inventoried**: 0.
- **`tests/e2e/smoke.spec.ts`**: Tests basic app loading and routing, but does not mock offline mode or service worker lifecycles.
- **brittleness**: Not applicable.

# 39. PWA support matrix

- **first-visit offline**: absent (Requires browser verification).
- **returning-user offline**: partial (Shell loads, data fails).
- **cached shell**: implemented.
- **local data**: implemented (`localStorage`).
- **offline logging**: partial (Saves locally, fails to sync).
- **reconnect**: absent.
- **install prompt**: absent.
- **standalone mode**: implemented (manifest).
- **update detection**: absent.
- **update prompt**: absent.
- **controlled activation**: absent (`skipWaiting` is immediate).
- **cache cleanup**: partial (Manual version bump required).
- **storage failure**: absent (Silent catch).
- **iOS metadata**: implemented.
- **Android metadata**: implemented.
- **desktop installation**: partial (Relies on default behavior).

# 40. Prioritized PWA risk register

- **Critical**: Stale app code remaining indefinitely. (Evidence: No update prompt, static cache name in `public/sw.js`). User consequence: Users use outdated versions.
- **High**: Cache quota failure without feedback. (Evidence: `try/catch` in `src/lib/persist.ts`). User consequence: Data silently fails to save.
- **High**: Mixed-version assets. (Evidence: HTML cached, JS hashed). User consequence: White screen offline.

# 41. Prioritized offline and update risk register

- **High**: Active workout lost on reload. (Evidence: No `beforeunload` protection or update prompt).
- **High**: Offline mutation falsely appears saved to cloud. (Evidence: Local writes succeed, but no background sync exists).
- **Medium**: Storage quota exceeded during offline mode.

# 42. Future implementation acceptance checklist

- [ ] manifest fields remain valid and consistent;
- [ ] required icon sizes are present;
- [ ] maskable icons are valid;
- [ ] HTML metadata matches the manifest;
- [ ] service-worker registration failures are handled;
- [ ] cache strategy is documented;
- [ ] old caches are cleaned safely (automated versioning);
- [ ] deep routes reload correctly;
- [ ] returning users can launch the cached shell offline where supported;
- [ ] first-visit offline behavior is honest;
- [ ] offline actions do not falsely report success;
- [ ] reconnect behavior does not duplicate records;
- [ ] stale data is labeled;
- [ ] update availability is detectable;
- [ ] waiting updates have a clear user-controlled path;
- [ ] active workouts are protected from unsafe reload;
- [ ] unsaved forms are protected from unsafe reload;
- [ ] mixed-version assets are prevented;
- [ ] storage quota failures are actionable;
- [ ] install prompts are accessible;
- [ ] offline and update surfaces work at 320 px;
- [ ] iOS safe areas are respected;
- [ ] Android maskable icons are valid;
- [ ] desktop standalone behavior is intentional;
- [ ] PWA tests cover lifecycle and failure states.

# 43. Future Data Safety integration checklist

- **offline mutation transactions**: current support level absent; requires robust queuing.
- **atomic local saves**: current support level partial (`localStorage`).
- **revision handling**: current support level absent.
- **reload during active workout**: current support level unsafe.
- **update activation during pending mutation**: current support level unsafe.
- **stale-write handling after reconnect**: current support level absent.
- **another-tab updates**: current support level absent.
- **storage failure**: current support level partial (silent catch).
- **corrupted local state**: current support level absent.

# 44. Safe future task boundaries

- **service-worker files requiring isolated ownership**: `public/sw.js`.
- **manifest files requiring isolated ownership**: `public/manifest.json`.
- **application bootstrap hotspots**: `src/routes/__root.tsx`.
- **tests likely to require coordination**: Playwright E2E smoke tests.
- **recommended sequencing boundaries**: Implement automated SW generation (Workbox) _before_ building UI update prompts.

# 45. Open questions and uncertainties

- Does iOS Safari reliably cache the offline shell given the current `skipWaiting` implementation? (Requires browser verification).
- Do maskable icons render correctly on Android? (Requires platform verification).
- Does the 404 error component safely render offline when JS is cached? (Requires offline simulation).

# 46. File index

- manifest: `public/manifest.json`
- HTML metadata: `src/routes/__root.tsx`
- service worker: `public/sw.js`
- registration: `src/routes/__root.tsx`
- build configuration: `vite.config.ts`
- application startup: `src/routes/__root.tsx`
- storage dependencies: `src/lib/persist.ts`
- storage dependencies: `src/lib/fitcore-data.ts`
- Jarvis: `src/components/app/jarvis/jarvis-panel.tsx`
- E2E tests: `tests/e2e/smoke.spec.ts`
- documentation references: `docs/audits/service-worker-cache-audit.md`
- icons:
  - `public/icon-192.png`
  - `public/icon-512.png`
  - `public/icon-maskable-192.png`
  - `public/icon-maskable-512.png`
  - `public/apple-touch-icon.png`
