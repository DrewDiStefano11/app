Oh, I had already committed the previous changes locally, but I didn't push. Wait, I did `git reset --hard HEAD` and it reset to my commit, so `active-workout.tsx` and `training.tsx` are currently correct except they have the wrong label.

I just need to update `src/components/app/views/training.tsx` to fix the button label "Start Plan" back to "Start today's plan", commit --amend, and push/submit.
